services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/commerce.db
    volumes:
      - commerce_data:/data

volumes:
  commerce_data:
